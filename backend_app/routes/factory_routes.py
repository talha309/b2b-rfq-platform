# Factory Routes — 4 routes

# GET /rfqs → View assigned RFQs

# POST /quotation → Submit quotation for a specific RFQ

# GET /quotation/{id} → View a quotation

# GET /orders → View confirmed customer orders