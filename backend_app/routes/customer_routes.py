# GET /products → View product catalog

# GET /factories → View partner factories

# POST /rfq → Submit new RFQ (from chatbot data or form)

# GET /rfq/{id} → View specific RFQ status

# GET /quotations → View received quotations from factories